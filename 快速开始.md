# 🚀 Tghao 批量同步快速开始

## ✅ 已完成的配置

1. ✅ 数据库已创建（db）
2. ✅ 初始数据已导入（管理员账号等）
3. ✅ Tghao 配置已启用
4. ✅ 同步工具脚本已创建

## 📝 管理员账号

- **用户名**: `admin`
- **密码**: `123456#`

## 🔧 当前配置

在 `restart-backend.ps1` 中：

```powershell
$env:TGHAO_ENABLED = "true"
$env:TGHAO_APP_ID = "2853"
$env:TGHAO_APP_KEY = "87690A94DD4EC45B"
```

## 🎯 使用同步工具

### 1. 确保后端运行

```powershell
# 如果后端未运行，启动它
.\restart-backend.ps1
```

### 2. 运行同步工具

```powershell
.\sync-products.ps1
```

### 3. 选择同步方式

工具会引导你选择：

- **[1] 全量同步** - 同步所有 Tghao 商品（推荐首次使用）
- **[2] 按分类同步** - 只同步特定分类的商品
- **[3] 精选同步** - 手动输入商品编码同步

## ⚠️ 当前问题

测试时发现 Tghao API 连接超时（"Unexpected end of file from server"）。

**可能原因：**
1. Tghao 服务器限流或繁忙
2. 网络连接问题
3. API 密钥权限限制

**解决方法：**
1. 稍后重试（等待几分钟）
2. 检查 Tghao 后台账户状态
3. 确认 API 密钥是否有效且有权限

## 🔄 手动 API 测试

如果同步工具失败，可以手动测试：

### 测试连接

```powershell
$token = "你的Token"  # 从同步工具获取

Invoke-RestMethod -Uri "http://localhost:8083/api/admin/tghao/test-connection" `
  -Method POST `
  -Headers @{ "Authorization" = "Bearer $token" }
```

### 获取商品列表

```powershell
Invoke-RestMethod -Uri "http://localhost:8083/api/admin/tghao/products" `
  -Method GET `
  -Headers @{ "Authorization" = "Bearer $token" }
```

## 📊 查看后端日志

如果需要查看详细错误信息：

```powershell
# 后端日志会输出 Tghao API 的详细请求和响应
# 查看最近的日志以诊断问题
```

## 🌐 访问商城

- **前台**: http://localhost:3000
- **后台**: http://localhost:3000/admin
- **API**: http://localhost:8083/api

## 💡 同步成功后

1. 访问 http://localhost:3000/admin 登录管理后台
2. 进入商品管理，查看同步的商品
3. 批量上架商品
4. 测试前台下单流程

## 🔑 自动发货流程

用户下单 → 支付成功 → 系统自动在 Tghao 下单 → 获取卡密 → 发货给用户

如果 Tghao 下单失败，会自动回退到本地库存。

## 📖 完整文档

详细说明请查看：`TGHAO_SYNC_GUIDE.md`

---

**注意**: 如果 Tghao API 持续超时，请检查：
1. Tghao 官网是否正常访问
2. 你的网络是否稳定
3. API 密钥是否正确且有效

成功同步后，所有商品会自动按 1.2 倍加价（20% 利润）上架。
