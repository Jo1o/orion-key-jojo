# 🚀 快速部署命令参考

## 📦 在服务器上部署

### 方法一：使用一键脚本（推荐）

```bash
# 1. 上传项目到服务器
scp -r D:\workspace\orion-key-jojo root@your-server-ip:/root/

# 2. 登录服务器
ssh root@your-server-ip

# 3. 进入项目目录
cd /root/orion-key-jojo

# 4. 赋予脚本执行权限
chmod +x deploy.sh

# 5. 运行一键部署
./deploy.sh
```

### 方法二：手动部署

```bash
# 1. 配置环境变量
cp .env.example .env
nano .env

# 2. 配置 SSL 证书（Let's Encrypt）
apt install certbot
certbot certonly --standalone -d jojobuy.top -d www.jojobuy.top
mkdir -p nginx/ssl
cp /etc/letsencrypt/live/jojobuy.top/fullchain.pem nginx/ssl/jojobuy.top.crt
cp /etc/letsencrypt/live/jojobuy.top/privkey.pem nginx/ssl/jojobuy.top.key

# 3. 构建并启动
docker-compose build
docker-compose up -d

# 4. 查看日志
docker-compose logs -f
```

## 🔧 常用维护命令

```bash
# 查看所有容器状态
docker-compose ps

# 查看实时日志
docker-compose logs -f

# 重启所有服务
docker-compose restart

# 重启单个服务
docker-compose restart api

# 停止服务
docker-compose stop

# 停止并删除容器（保留数据）
docker-compose down

# 更新代码后重新部署
git pull
docker-compose build
docker-compose up -d

# 备份数据库
docker exec orion-postgres pg_dump -U postgres db > backup.sql

# 恢复数据库
docker exec -i orion-postgres psql -U postgres -d db < backup.sql
```

## 🔑 默认账号

- **用户名**: admin
- **密码**: admin123
- **首次登录后请立即修改密码！**

## 🌐 访问地址

- 前台：https://jojobuy.top
- 后台：https://jojobuy.top/admin
- API：https://jojobuy.top/api

## 📖 详细文档

查看 `DEPLOY.md` 获取完整部署文档。
